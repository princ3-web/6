import React from "react";
import { useInView } from "react-intersection-observer";
import { Box } from "@mui/material";

const Animated = () => {
  return <Box>Animated</div>;
};

export default Animated;
